apiVersion: apps/v1
kind: Deployment
metadata:
  name: socket-service
  namespace: leave-management
spec:
  replicas: 2
  selector:
    matchLabels:
      app: socket-service
  template:
    metadata:
      labels:
        app: socket-service
    spec:
      initContainers:
        - name: wait-for-mongodb
          image: mongo:latest
          command:
            - sh
            - -c
          args:
            - until mongosh --host mongodb --eval "db.adminCommand('ping')" >/dev/null 2>&1; do sleep 2; done
      containers:
        - name: socket-service
          image: lms-socket-service:ci
          imagePullPolicy: Never
          ports:
            - containerPort: 1003
          envFrom:
            - configMapRef:
                name: app-configmap
            - secretRef:
                name: app-secret
---
apiVersion: v1
kind: Service
metadata:
  name: socket-service
  namespace: leave-management
spec:
  type: NodePort
  selector:
    app: socket-service
  ports:
    - port: 1003
      targetPort: 1003
      nodePort: 31003
